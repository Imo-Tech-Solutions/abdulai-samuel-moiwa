<p style="color: green"><%= notice %></p>

<%= render @aset %>

<div>
  <div class="edit_back_link">
    <%= link_to "Edit this aset", edit_aset_path(@aset) %> |
    <%= link_to "Back to assets", asets_path %>
  </div>

  <%= button_to "Destroy this asset", @aset, method: :delete, class: "distory_asset" %>
</div>
